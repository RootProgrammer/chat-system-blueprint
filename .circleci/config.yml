# This config was automatically generated from your source code
# Stacks detected: cicd:github-actions:.github/workflows,deps:python:backend/auth_service,file:manage.py:backend/auth_service
version: 2.1
orbs:
  python: circleci/python@2
jobs:
  test-python:
    # Install dependencies and run tests
    docker:
      - image: cimg/python:3.8-node
    working_directory: ~/project/backend/auth_service
    steps:
      - checkout:
          path: ~/project
      - python/install-packages
      - run:
          name: Run tests
          command: python manage.py test
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
      - run:
          name: Trigger Jira Webhook
          command: |
            curl -X POST -H "Content-Type: application/json" -d '{"key":"value"}' $JIRA_WEB_TRIGGER_URL
      # - run:
      #     name: found github actions config
      #     command: ':'
workflows:
  build-and-test:
    jobs:
      - test-python
    # - deploy:
    #     requires:
    #       - test-python
